{
  "name": "ovi-assistai",
  "version": "1.0.0",
  "description": "Deployment configuration package",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build"
  }
}
